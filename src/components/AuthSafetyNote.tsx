/**
 * Kept as a named re-export so the auth screens read clearly, but the
 * implementation and the wording now live in one place. See ui/SafetyNote for
 * why: this sentence was duplicated across 19 files and rendered 16 times on a
 * single page.
 */
export { default } from './ui/SafetyNote';
